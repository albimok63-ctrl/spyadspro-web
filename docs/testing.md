## Docker Readiness Tests

La suite di test garantisce che:
- l'app FastAPI è importabile
- le dipendenze sono installate correttamente
- l'endpoint /health risponde
- la struttura del progetto è valida
- la build Docker non fallisce per errori di runtime
