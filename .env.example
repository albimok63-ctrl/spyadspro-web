# Configurazione centralizzata (app/core/config.py). Copiare in .env e valorizzare.

# Segreto principale (JWT, signing). In produzione usare valore sicuro (≥32 caratteri).
SECRET_KEY=change-me-in-production-min-32-chars
# Scadenza token di accesso (minuti).
ACCESS_TOKEN_EXPIRE_MINUTES=60
# Ambiente: development | staging | production
ENVIRONMENT=development

APP_ENV=local
REDIS_HOST=redis
REDIS_PORT=6379
